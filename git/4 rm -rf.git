mkdir will create a folder  
=> mkdir delete

=> git status
fatal: not a git repository (or any of the parent directories): .git

[the folder created is still not a git repo]
=>git init

=> rm -rf .git
(it will delete the content on .git file but dir will stay)